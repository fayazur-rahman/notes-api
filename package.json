{
  "name": "minimal-notes-api",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Express + PostgreSQL REST API for notes",
  "type": "module",
  "engines": {
    "node": ">=20.6.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --env-file=.env src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "5.2.1",
    "pg": "8.23.0"
  }
}
